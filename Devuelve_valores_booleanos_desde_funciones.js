/* Una mejor forma de hacer una comparacion como
esta */

function isEqual(a, b) {
    if (a === b) {
      return true;
    } else {
      return false;
    }
  }

  /* Puede ser de la siguiente manera */

  function isEqual(a, b) {
    return a === b;
  }

  /*Esto es porque "===" devuelve "true" o "false" */

  